# AgentScope 核心（指定 2.0.3 版本）
agentscope==2.0.3
mcp>=1.28.0

# AgentScope Runtime（用于部署 Agent 服务）
agentscope-runtime

# Web 服务框架
fastapi
uvicorn

# 会话存储（开发/测试用 FakeRedis，生产可换真实 Redis）
fakeredis
redis

# 可选：使用 OpenAI SDK 调用部署后的服务
openai
langchain_openai
langgraph